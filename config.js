// config.js

export default async ()=>{
    const config = {
        siteTitle: 'យើង​លក់​ឥវ៉ាន់',
        message: '',
        maxPosts: 10,
        frontPosts: 8,
        relatedPosts: 5,
        maxCategories: 12,
        maxSearch: 20,
    }

    return config
}
